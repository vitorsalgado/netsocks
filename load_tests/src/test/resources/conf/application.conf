gatling {
  rampUp = 15
  maxDuration = 600
  repeat = 2
  users = 100
  size = 20
  pagination = 10
  includeScenarios = "_all"
  excludeScenarios = ""
}

campaign_api {
  index = ""
  http {
    base = "localhost:8080"
    path = "/"
    connTimeout = 5000
    readTimeout = 2000
  }
}
